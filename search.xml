<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>一个编译宏引发的 bug</title>
    <url>/fuchencong.github.io/2021/04/20/cpp-develop-notes-1/</url>
    <content><![CDATA[<p>昨天使用厂内 bthread 库（来源于 brpc，已开源）中的 TimerThread 工具来做定时重试任务，但是在打印返回的 TimerThread ID 时，始终打印的是 0，而 TimerThread 分明已经启动成功了。定位了将近一天，才找到原因，这里分享一下。</p>
<span id="more"></span>

<h2 id="问题现象"><a href="#问题现象" class="headerlink" title="问题现象"></a>问题现象</h2><p>问题可以简化为如下代码：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">thread::TimerThread temp_thread;</span><br><span class="line"><span class="keyword">if</span> (temp_thread.start(NULL)) &#123;</span><br><span class="line">    LOG(INFO) &lt;&lt; <span class="string">&quot;start failed&quot;</span>;</span><br><span class="line">&#125;</span><br><span class="line">LOG(INFO) &lt;&lt; <span class="string">&quot;Temp Thread id: &quot;</span> &lt;&lt; <span class="string">temp_thread.thread_id();</span></span><br></pre></td></tr></table></figure>

<p>TimerThread 的 start 函数返回成功，但是调用 thread_id() 始终返回 0：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">TRACE: 04-20 18:43:22:   * 0 baidu/vnet/dedicatedconn/src/dedicatedconn_cli_main.cpp:1732] Temp Thread id: 0</span><br></pre></td></tr></table></figure>

<p>TimerThread 的 start 函数内部会调用 <code>pthread_create</code> 创建一个线程，通过在 start 函数内部添加打印，可以看到 _thread 已经被正确赋值了：</p>
<p>TimerThread 的 start 函数实现部分：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">...</span><br><span class="line">const int ret = pthread_create(&amp;_thread, NULL, TimerThread::run_this, this);</span><br><span class="line"><span class="keyword">if</span> (ret) &#123;</span><br><span class="line">    <span class="built_in">return</span> ret;</span><br><span class="line">&#125;</span><br><span class="line">_started = <span class="literal">true</span>;</span><br><span class="line">LOG(INFO) &lt;&lt; <span class="string">&quot;In bthread Timer thread id &quot;</span> &lt;&lt; <span class="string">_thread;</span></span><br></pre></td></tr></table></figure>

<p>TimeThread 的内部打印，thread_id 是正确的：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">TRACE: 04-20 18:43:22:   * 0 baidu/base/bthread/bthread/timer_thread.cpp:163] In bthread Timer thread id 140271338514176</span><br></pre></td></tr></table></figure>

<p>由于 thread_id 函数就是直接返回的 _thread 成员：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">pthread_t thread_id() const &#123; <span class="built_in">return</span> _thread; &#125;</span><br></pre></td></tr></table></figure>

<p>那为啥成员函数内部打印的结果和调用 thread_id public 函数返回的结果不一致呢，难道碰到了玄学问题?</p>
<h2 id="定位问题"><a href="#定位问题" class="headerlink" title="定位问题"></a>定位问题</h2><p>为了定位该问题，将 TimerThread 类的 private 成员直接修改为 public 成员，然后在我自己的程序和 TimerThread 的 start 函数里分别打印对象地址、成员偏移量、成员大小，得到一个重要线索：TimerThread 类里 _mutex 的大小不一致：</p>
<p>应用程序打印：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">TRACE: 04-20 18:43:22:   * 0 baidu/vnet/dedicatedconn/src/dedicatedconn_cli_main.cpp:1740] Timer _mutex size 40</span><br></pre></td></tr></table></figure>

<p>TimerThread 内部打印：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">TRACE: 04-20 18:43:22:   * 0 baidu/base/bthread/bthread/timer_thread.cpp:171] Timer _mutex size 4</span><br></pre></td></tr></table></figure>

<p>由于 _thread 成员定义在 _mutex 成员后面，这就导致了 _thread 在类对象内部的偏移量也不一致。这就造成了对于同一个类对象，两边看到的 _thread 偏移不同，得到的值也就不同。</p>
<p>那为啥 _mutex 的大小两边不一致呢，原来  _mutex 的类型通过了一个编译宏控制：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">internal::FastPthreadMutex _mutex;    // protect _nearest_run_time</span><br><span class="line"></span><br><span class="line">namespace internal &#123;</span><br><span class="line"><span class="comment">#ifdef BTHREAD_USE_FAST_PTHREAD_MUTEX</span></span><br><span class="line">class FastPthreadMutex &#123;</span><br><span class="line">public:</span><br><span class="line">    FastPthreadMutex() : _futex(0) &#123;&#125;</span><br><span class="line">    ~<span class="function"><span class="title">FastPthreadMutex</span></span>() &#123;&#125;</span><br><span class="line">    void lock();</span><br><span class="line">    void unlock();</span><br><span class="line">    bool try_lock();</span><br><span class="line">private:</span><br><span class="line">    DISALLOW_COPY_AND_ASSIGN(FastPthreadMutex);</span><br><span class="line">    int lock_contended();</span><br><span class="line">    unsigned _futex;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="comment">#else</span></span><br><span class="line">typedef base::Mutex FastPthreadMutex;</span><br><span class="line"><span class="comment">#endif</span></span><br></pre></td></tr></table></figure>

<p>由于 bthread 库编译的 Makefile 中通过 <code>-DBTHREAD_USE_FAST_PTHREAD_MUTEX</code> 定义了该编译宏，而自己的应用程序编译时没有定义该宏，这就导致两边看到的 TimerThread 类的定义并不完全一致（ _mutex 成员大小不一致）。</p>
<p>不知道大家有没有想过一个问题，即使类型定义不一致，但是在应用程序中我并不是直接访问的 _thread 成员，而是调用 thread_id 成员函数得到 _thread 的值。那在调用 TimerThread 的成员函数时，应该是按照 TimerThread 自己所看到的定义返回 _thread 的值，为啥实际上也没有返回正确结果呢？原因就是 thread_id 函数是在类内定义的，所以它是一个内联函数。这就使得它在函数调用点直接内联展开，这就和直接在应用程序访问 _thread 成员效果是等价的。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">pthread_t thread_id() const &#123; <span class="built_in">return</span> _thread; &#125;</span><br></pre></td></tr></table></figure>

<h2 id="解决问题"><a href="#解决问题" class="headerlink" title="解决问题"></a>解决问题</h2><p>在应用程序侧加上 BTHREAD_USE_FAST_PTHREAD_MUTEX 编译宏定义，thread id 打印成功，问题解决。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">TRACE: 04-20 20:17:04:   * 0 baidu&#x2F;base&#x2F;bthread&#x2F;bthread&#x2F;timer_thread.cpp:163] In bthread Timer thread id 139925091317504</span><br><span class="line">TRACE: 04-20 20:17:04:   * 0 baidu&#x2F;vnet&#x2F;dedicatedconn&#x2F;src&#x2F;dedicatedconn_cli_main.cpp:1732] Temp Thread id: 139925091317504</span><br></pre></td></tr></table></figure>

<h2 id="进一步验证"><a href="#进一步验证" class="headerlink" title="进一步验证"></a>进一步验证</h2><p>这里我写了一个简单的验证程序，该程序包含三个文件：</p>
<p>common.h</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">A</span> &#123;</span></span><br><span class="line">    <span class="keyword">int</span> a[<span class="number">10</span>];</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">ifdef</span> BTHREAD_USE_FAST_PTHREAD_MUTEX</span></span><br><span class="line"><span class="keyword">typedef</span> <span class="keyword">int</span> LOCK;</span><br><span class="line"><span class="meta">#<span class="meta-keyword">else</span></span></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">A</span> <span class="title">LOCK</span>;</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">endif</span></span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">TimerThread</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">TimerThread</span>();</span><br><span class="line">    <span class="function"><span class="keyword">int</span> <span class="title">get_magic</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        std::cout &lt;&lt; <span class="string">&quot;In TimerThread sizeof(t) is &quot;</span> &lt;&lt; <span class="built_in"><span class="keyword">sizeof</span></span>(*<span class="keyword">this</span>) &lt;&lt; std::endl;</span><br><span class="line">        <span class="keyword">return</span> magic_number;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    LOCK l;</span><br><span class="line">    <span class="keyword">int</span> magic_number;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>common.cpp</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&quot;common.h&quot;</span></span></span><br><span class="line"></span><br><span class="line">TimerThread::<span class="built_in">TimerThread</span>(): <span class="built_in">magic_number</span>(<span class="number">12345</span>) &#123;&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<p>use_common.cpp</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment">#include &lt;iostream&gt;</span></span><br><span class="line"><span class="comment">#include &quot;common.h&quot;</span></span><br><span class="line"></span><br><span class="line">int main(void)</span><br><span class="line">&#123;</span><br><span class="line">    TimerThread t;</span><br><span class="line">    std::cout &lt;&lt; <span class="string">&quot;In main function: direct get magic number &quot;</span> &lt;&lt; <span class="string">t.magic_number &lt;&lt; std::endl;</span></span><br><span class="line"><span class="string">    std::cout &lt;&lt; &quot;In main function: sizeof is &quot; &lt;&lt; sizeof(t</span>) &lt;&lt; <span class="string">std::endl;</span></span><br><span class="line"><span class="string">    std</span>::cout &lt;&lt; <span class="string">&quot;In main function: call get_magic  &quot;</span> &lt;&lt; <span class="string">t.get_magic() &lt;&lt; std::endl;</span></span><br><span class="line"><span class="string">&#125;</span></span><br></pre></td></tr></table></figure>

<p>按照如下方式生成可执行文件：</p>
<ol>
<li>编译 common.o，定义 BTHREAD_USE_FAST_PTHREAD_MUTEX 编译宏：</li>
</ol>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">g++ -g -c common.cpp -DBTHREAD_USE_FAST_PTHREAD_MUTEX</span><br></pre></td></tr></table></figure>

<ol start="2">
<li>编译 use_common.o，不定义 BTHREAD_USE_FAST_PTHREAD_MUTEX 宏：</li>
</ol>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">g++ -g -c use_common.cpp</span><br></pre></td></tr></table></figure>

<ol start="3">
<li>生成可执行程序：</li>
</ol>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">g++ -g -o use_common common.o use_common.o</span><br></pre></td></tr></table></figure>

<p>执行该程序可以看到，无论是直接访问 magic_number，还是调用内联成员函数，都没有得到正确的值。且即使在内联函数内部，得到的 TimerThread 大小也和应用程序看到的一致：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">./use_common</span><br><span class="line">In main <span class="keyword">function</span>: direct get magic number 4196288</span><br><span class="line">In main <span class="keyword">function</span>: sizeof is 44</span><br><span class="line">In TimerThread sizeof(t) is 44</span><br><span class="line">In main <span class="keyword">function</span>: call get_magic  4196288</span><br></pre></td></tr></table></figure>

<p>接下来将 get_magic 定义为非内联函数，即：将 get_magic 的定义放到 common.cpp 中，其他代码保持不变，编译方法也保持不变，再次运行该程序，运行结果如下：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ ./use_common</span><br><span class="line">In main <span class="keyword">function</span>: direct get magic number 4196288</span><br><span class="line">In main <span class="keyword">function</span>: sizeof is 44</span><br><span class="line">In TimerThread sizeof(t) is 8</span><br><span class="line">In main <span class="keyword">function</span>: call get_magic  12345</span><br></pre></td></tr></table></figure>

<ul>
<li>在应用程序内直接访问 magic_number 成员变量得到错误值，这是因为应用程序编译时看到的 TimerThread 类型定义和该对象的实际内存分布并不一致</li>
<li>在应用程序内调用 get_magic 成员函数返回的 magic_number 为正确值，这是因为 get_magic 函数是在编译 common.o 时编译的get_magic 函数的确能够按照该对象构造时的定义解释这个对象，所以能够得到正确结果</li>
<li>在应用程序内看到的 TimerThread 大小为 44，而在 TimerThread 成员函数内部看到的对象大小为 8。sizeof 的结果在编译器就已经决定了，符合预期</li>
</ul>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>当同一个类对象的成员变量在不同的编译单元中显示为不同的值时，应该重点排查这些编译单元看到的类型定义是完全一致，造成不一致的可能原因有：</p>
<ul>
<li>包含的头文件版本不同</li>
<li>类型的定义受编译宏控制，而不同编译单元可能使用不同的编译宏</li>
</ul>
]]></content>
      <categories>
        <category>C++ 开发笔记</category>
      </categories>
      <tags>
        <tag>C++</tag>
      </tags>
  </entry>
  <entry>
    <title>使用 gperftools 检测内存泄露</title>
    <url>/fuchencong.github.io/2021/04/22/develop-tools-1/</url>
    <content><![CDATA[<p>每个 C/C++ 程序员可能都经历过定位内存泄露问题的痛苦。一方面，为了减少内存泄露问题，在 C++ 程序中，我们应该尽量使用智能指针，在 C 程序中，我们也可以通过一些内存池技术来管理内存的申请与释放。另一方面，当内存泄露问题真的出现时，通过 gperftools 的 <code>heap checker</code>，我们也可以比较轻松地找到内存泄露的线索，缩短问题定位时间。</p>
<span id="more"></span>

<h2 id="gperftools-简介"><a href="#gperftools-简介" class="headerlink" title="gperftools 简介"></a>gperftools 简介</h2><p>gperftools 是 google 开源的一组套件，提供了高性能的、支持多线程的 malloc 实现，以及一组优秀的性能分析工具。</p>
<h2 id="安装-gperftools"><a href="#安装-gperftools" class="headerlink" title="安装 gperftools"></a>安装 gperftools</h2><p>接下来介绍如何从源码安装 gperftools：</p>
<h3 id="下载源码"><a href="#下载源码" class="headerlink" title="下载源码"></a>下载源码</h3><p>从 gperftools github 官网上下载最新版本的源码包：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">$ wget https:<span class="comment">//github.com/gperftools/gperftools/releases/download/gperftools-2.9.1/gperftools-2.9.1.tar.gz</span></span><br></pre></td></tr></table></figure>

<h3 id="解压源码包"><a href="#解压源码包" class="headerlink" title="解压源码包"></a>解压源码包</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">$ tar -zxv -f gperftools<span class="number">-2.9</span><span class="number">.1</span>.tar.gz</span><br></pre></td></tr></table></figure>

<h3 id="configure"><a href="#configure" class="headerlink" title="configure"></a>configure</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ cd gperftools-2.9.1</span><br><span class="line">$ .&#x2F;configure</span><br></pre></td></tr></table></figure>

<p>命令结束执行后出现一个报错：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">configure: WARNING: No frame pointers <span class="keyword">and</span> no libunwind. Using experimental backtrace capturing via libgcc. Expect crashy cpu profiler.</span><br></pre></td></tr></table></figure>

<p>这是因为没有安装 <code>libunwind</code>。这里直接使用 yum 的方式安装：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">yum install libunwind-devel</span><br></pre></td></tr></table></figure>

<p>再次执行 <code>./configure</code>，命令执行成功。</p>
<h3 id="编译并安装"><a href="#编译并安装" class="headerlink" title="编译并安装"></a>编译并安装</h3><p>执行如下两个命令，进行编译并安装：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">make</span><br><span class="line">sudo make install</span><br></pre></td></tr></table></figure>

<p>最后执行 <code>ldconfig</code> 更新动态库文件</p>
<h3 id="确认安装成功"><a href="#确认安装成功" class="headerlink" title="确认安装成功"></a>确认安装成功</h3><p>执行如下命令，确认 gperftools 安装成功</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">[root@<span class="number">36</span>eab106d3bf gperftools<span class="number">-2.9</span><span class="number">.1</span>]<span class="meta"># pprof --version</span></span><br><span class="line"><span class="built_in">pprof</span> (part of gperftools <span class="number">2.0</span>)</span><br><span class="line"></span><br><span class="line">Copyright <span class="number">1998</span><span class="number">-2007</span> Google Inc.</span><br><span class="line"></span><br><span class="line">This is BSD licensed software; see the source <span class="keyword">for</span> copying conditions</span><br><span class="line"><span class="keyword">and</span> license information.</span><br><span class="line">There is NO warranty; <span class="keyword">not</span> even <span class="keyword">for</span> MERCHANTABILITY <span class="keyword">or</span> FITNESS FOR A</span><br><span class="line">PARTICULAR PURPOSE.</span><br></pre></td></tr></table></figure>

<h2 id="内存泄露检测"><a href="#内存泄露检测" class="headerlink" title="内存泄露检测"></a>内存泄露检测</h2><p>gperftools 的 <code>heap checker</code> 组件可以用于检测 C++ 程序中的内存泄露问题。要使用 <code>heap checker</code>，总共分 3 步：</p>
<ul>
<li>链接 tcmalloc 库到应用程序中</li>
<li>运行程序</li>
<li>分析输出</li>
</ul>
<h3 id="链接-tcmalloc-库"><a href="#链接-tcmalloc-库" class="headerlink" title="链接 tcmalloc 库"></a>链接 tcmalloc 库</h3><p><code>heapchecker</code> 是 tcmalloc 的一部分，所以为了在可执行程序中使用 <code>heap checker</code>，在应用程序的链接阶段使用 <code>-ltcmalloc</code> 链接 tcmalloc 库。</p>
<h3 id="运行代码"><a href="#运行代码" class="headerlink" title="运行代码"></a>运行代码</h3><p>使用 <code>heap checker</code> 的推荐方式是 <code>完整程序运行模式</code>，此时 <code>heap checker</code> 可以在程序的 main 函数开始前跟踪内存分配，然后在程序退出时再次检查。如果它发现来了任何内存泄露，<code>heap checker</code> 会直接终止程序（通过 exit(1))，并打印一条消息，告诉你接下来如何使用 pprof 来继续跟踪该内存泄露问题。</p>
<p><code>完整程序运行</code> 的 <code>heap checker</code> 支持 4 种模式：</p>
<ul>
<li>minimal：在程序初始化期间（即 main 函数运行前）不进行内存泄露检查</li>
<li>normal：正常模式，通过跟踪某块内存是否可以被 <code>live object</code> 来访问，来判断是否出现内存泄露</li>
<li>strict：类似于 <code>normal</code> 模式，但是对全局对象的内存泄露有一些额外的检查</li>
<li>draconian：在该模式下，只有所有申请的内存都被释放，才认为没有出现内存泄露</li>
</ul>
<p>一般使用 <code>normal</code> 模式就可以满足日常要求了。</p>
<p><code>heap checker</code> 的另一种使用方法是检测指定代码块是否出现了内存泄露。为了实现这一点，需要在代码片段的开始部分创建一个 <code>HeapLeakChecker</code> 结构体，并在结束部分调用 <code>NoLeaks()</code>。例如：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function">HeapLeakChecker <span class="title">heap_checker</span><span class="params">(<span class="string">&quot;test_foo&quot;</span>)</span></span>;</span><br><span class="line">&#123;</span><br><span class="line">  code that exercises some foo functionality;</span><br><span class="line">  <span class="keyword">this</span> code should <span class="keyword">not</span> leak memory;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">if</span> (!heap_checker.<span class="built_in">NoLeaks</span>()) <span class="built_in">assert</span>(<span class="literal">NULL</span> == <span class="string">&quot;heap memory leak&quot;</span>);</span><br></pre></td></tr></table></figure>

<p>需要注意，添加 <code>HeapLeakChecker</code> 只是在程序中添加了内存泄露检测代码，为了真实地检测程序是否出现了内存泄露，仍然需要运行程序，并打开 heap-checker。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">env HEAPCHECK=local your_program</span><br></pre></td></tr></table></figure>

<p>除了指定为 local 模式外，之前的 <code>normal</code> 等模式也是可以的，此时除了运行 <code>local</code> 检查外，还将进行 <code>完整程序运行</code> 检查。</p>
<p>当然，运行 <code>heap checker</code> 是有代价的。<code>heap checker</code> 需要记录每次内存申请时的调用栈信息，这就导致了使用 <code>heap checker</code> 时，程序需要消耗更多的内存，同时程序运行速度也更慢。另外，需要注意，由于 <code>heap checker</code> 内部使用了 <code>heap profile</code> 框架，所以不能同时运行 <code>heap checker</code> 和 <code>heap profile</code>。</p>
<h3 id="忽略已知的内存泄露"><a href="#忽略已知的内存泄露" class="headerlink" title="忽略已知的内存泄露"></a>忽略已知的内存泄露</h3><p>对于已知的内存泄露，如果想让 <code>heap checker</code> 忽略这些内存泄露信息，可以在应用程序代码中添加中如下代码：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">    HeapLeakChecker::Disabler disabler;</span><br><span class="line">    &lt;leaky code&gt;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>另一种方式是使用 IgnoreObject，它接收一个指针参数，对该参数所指向的对象将不再进行内存泄露检查。</p>
<h3 id="使用-pprof-查看内存泄露结果"><a href="#使用-pprof-查看内存泄露结果" class="headerlink" title="使用 pprof 查看内存泄露结果"></a>使用 pprof 查看内存泄露结果</h3><p><code>heap checker</code> 运行结束时会打印基本的泄露信息，包括调用栈和泄露对象的地址。除此之外，还可以使用 <code>pprof</code> 命令行工具来可视化地查看调用栈。</p>
<h2 id="示例"><a href="#示例" class="headerlink" title="示例"></a>示例</h2><p>接下来通过一个示例讲述如何使用 gperftools 的 <code>heap checker</code> 来发现程序的内存泄露问题。</p>
<ol>
<li>示例程序如下：</li>
</ol>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Copyright (C) fuchencong.com</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">func</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">int</span> *p = <span class="keyword">new</span> <span class="built_in"><span class="keyword">int</span></span>(<span class="number">10</span>);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    std::cout &lt;&lt; <span class="string">&quot;memory leak test&quot;</span> &lt;&lt; std::endl;</span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">func</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ol start="2">
<li>编译程序，并链接 <code>tcmalloc</code> 库：</li>
</ol>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">$ g++ -std=c++<span class="number">0</span>x -g -o memory_leak memory_leak.cpp -ltcmalloc</span><br></pre></td></tr></table></figure>

<ol start="3">
<li>运行程序</li>
</ol>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">[root@<span class="number">36</span>eab106d3bf gperftools-test]<span class="meta"># env HEAPCHECK=normal ./memory_leak</span></span><br><span class="line">WARNING: Perftools heap leak checker is active -- Performance may suffer</span><br><span class="line">memory leak test</span><br><span class="line">Have memory regions w/o callers: might report <span class="literal">false</span> leaks</span><br><span class="line">Leak check _main_ detected leaks of <span class="number">4</span> bytes in <span class="number">1</span> objects</span><br><span class="line">The <span class="number">1</span> largest leaks:</span><br><span class="line">*** WARNING: Cannot convert addresses to symbols in output below.</span><br><span class="line">*** Reason: Cannot find &#x27;pprof&#x27; (is PPROF_PATH set correctly?)</span><br><span class="line">*** If you cannot fix <span class="keyword">this</span>, <span class="keyword">try</span> running pprof directly.</span><br><span class="line">Leak of <span class="number">4</span> bytes in <span class="number">1</span> objects allocated from:</span><br><span class="line">	@ <span class="number">4008f</span>f</span><br><span class="line">	@ <span class="number">400935</span></span><br><span class="line">	@ <span class="number">7f</span>c9f81a6555</span><br><span class="line">	@ <span class="number">400829</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">If the preceding stack traces are <span class="keyword">not</span> enough to find the leaks, <span class="keyword">try</span> running THIS shell command:</span><br><span class="line"></span><br><span class="line">pprof ./memory_leak <span class="string">&quot;/tmp/memory_leak.16582._main_-end.heap&quot;</span> --inuse_objects --lines --heapcheck  --edgefraction=<span class="number">1e-10</span> --nodefraction=<span class="number">1e-10</span> --gv</span><br><span class="line"></span><br><span class="line">If you are still puzzled about why the leaks are there, <span class="keyword">try</span> rerunning <span class="keyword">this</span> program with HEAP_CHECK_TEST_POINTER_ALIGNMENT=<span class="number">1</span> <span class="keyword">and</span>/<span class="keyword">or</span> with HEAP_CHECK_MAX_POINTER_OFFSET=<span class="number">-1</span></span><br><span class="line">If the leak report occurs in a small fraction of runs, <span class="keyword">try</span> running with TCMALLOC_MAX_FREE_QUEUE_SIZE of few hundred MB <span class="keyword">or</span> with TCMALLOC_RECLAIM_MEMORY=<span class="literal">false</span>, it might help find leaks m</span><br><span class="line">Exiting with error <span class="built_in">code</span> (instead of crashing) because of whole-program memory leaks</span><br></pre></td></tr></table></figure>

<p>这里虽然检测出了内存泄露，但是并没有打印出调用栈的符号信息，根据提示，可能是 PPROF_PATH 环境变量没有正确设置。按照如下方式设置环境变量：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta"># echo $PPROF_PATH</span></span><br><span class="line"><span class="meta"># which pprof</span></span><br><span class="line">/usr/local/bin/pprof</span><br><span class="line"><span class="meta"># export PPROF_PATH=/usr/local/bin/pprof</span></span><br></pre></td></tr></table></figure>

<p>再次运行，可以看到已经打印出了内存泄露的栈信息：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">[root@<span class="number">36</span>eab106d3bf gperftools-test]<span class="meta"># env HEAPCHECK=normal ./memory_leak</span></span><br><span class="line">WARNING: Perftools heap leak checker is active -- Performance may suffer</span><br><span class="line">memory leak test</span><br><span class="line">Have memory regions w/o callers: might report <span class="literal">false</span> leaks</span><br><span class="line">Leak check _main_ detected leaks of <span class="number">4</span> bytes in <span class="number">1</span> objects</span><br><span class="line">The <span class="number">1</span> largest leaks:</span><br><span class="line">Using local file ./memory_leak.</span><br><span class="line">Leak of <span class="number">4</span> bytes in <span class="number">1</span> objects allocated from:</span><br><span class="line">	@ <span class="number">4008f</span>f func</span><br><span class="line">	@ <span class="number">400935</span> main</span><br><span class="line">	@ <span class="number">7f</span>39d94db555 __libc_start_main</span><br><span class="line">	@ <span class="number">400829</span> _start</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">If the preceding stack traces are <span class="keyword">not</span> enough to find the leaks, <span class="keyword">try</span> running THIS shell command:</span><br><span class="line"></span><br><span class="line">pprof ./memory_leak <span class="string">&quot;/tmp/memory_leak.16586._main_-end.heap&quot;</span> --inuse_objects --lines --heapcheck  --edgefraction=<span class="number">1e-10</span> --nodefraction=<span class="number">1e-10</span> --gv</span><br><span class="line"></span><br><span class="line">If you are still puzzled about why the leaks are there, <span class="keyword">try</span> rerunning <span class="keyword">this</span> program with HEAP_CHECK_TEST_POINTER_ALIGNMENT=<span class="number">1</span> <span class="keyword">and</span>/<span class="keyword">or</span> with HEAP_CHECK_MAX_POINTER_OFFSET=<span class="number">-1</span></span><br><span class="line">If the leak report occurs in a small fraction of runs, <span class="keyword">try</span> running with TCMALLOC_MAX_FREE_QUEUE_SIZE of few hundred MB <span class="keyword">or</span> with TCMALLOC_RECLAIM_MEMORY=<span class="literal">false</span>, it might help find leaks m</span><br><span class="line">Exiting with error <span class="built_in">code</span> (instead of crashing) because of whole-program memory leaks</span><br></pre></td></tr></table></figure>

<h2 id="Reference"><a href="#Reference" class="headerlink" title="Reference"></a>Reference</h2><ul>
<li><a href="https://github.com/couchbase/gperftools/blob/master/INSTALL">gperftools install</a></li>
<li><a href="https://gperftools.github.io/gperftools/heap_checker.html">gperftools heap checker</a></li>
</ul>
]]></content>
      <categories>
        <category>开发工具</category>
      </categories>
      <tags>
        <tag>gperftools</tag>
      </tags>
  </entry>
  <entry>
    <title>Screen 使用指南</title>
    <url>/fuchencong.github.io/2021/04/24/develop-tools-2/</url>
    <content><![CDATA[<p>Linux 程序员通常需要打开多个终端窗口连接到远端服务器，虽然现在的终端程序都比较好用，可以让你在多窗口间自如地切换。但是一旦你的网络断了，所有窗口上的远程连接也就断了，下一秒你不得不重新在各个窗口上进行重连。</p>
<p>通过 Screen，你可以在服务器上建立一个多窗口的会话，即使本地 PC 断网，重连服务器之后，直接恢复该 Screen 会话即可。本篇文章介绍 Screen 的基本用法。</p>
<span id="more"></span>

<h2 id="Screen-简介"><a href="#Screen-简介" class="headerlink" title="Screen 简介"></a>Screen 简介</h2><p>Screen 是一个全屏窗口管理器，可以在多个进程（通常是多个交互式shell）之间复用同一个物理终端。每一个虚拟终端都提供类似于物理终端的功能。当执行 screen 命令时，它会创建一个窗口，并在该窗口中创建一个 shell。之后可以继续执行以下操作：</p>
<ul>
<li>创建新的窗口并在新窗口中运行 shell</li>
<li>关闭窗口</li>
<li>查看活跃窗口列表</li>
<li>在窗口之间进行切换</li>
<li>在窗口之间复制文本</li>
<li>….</li>
</ul>
<p>所有窗口之间运行的程序完全相互独立，窗口间的切换并不影响窗口中程序的执行，甚至即使整个 screen 会话被 detach，窗口中的程序仍然会保持运行。当窗口中的程序执行完毕时，screen 默认会终止该程序所在的窗口。如果所有的窗口都被终止了，screen 会话则终止。</p>
<h2 id="配置-Screen"><a href="#配置-Screen" class="headerlink" title="配置 Screen"></a>配置 Screen</h2><p>有三种方式可以对 Screen 进行配置：</p>
<ul>
<li>修改 <code>.screenrc</code>：Screen 启动时会读取 home 目录下的 <code>.screenrc</code> 文件，对 screen 进行配置。该配置文件的中命令可以用于设置 screen 选项、绑定快捷键、或者在 screen 会话启动后自动建立多个 window 等等。Linux 发行版自带一个示例配置： <code>/etc/screenrc</code></li>
<li><code>source file</code>：从 file 中读取并执行 Screen 命令</li>
<li>colon 模式：通过 <code>C-a :</code> 进入 collon 模式，之后就可以直接执行 Screen 命令</li>
</ul>
<h2 id="Screen-会话"><a href="#Screen-会话" class="headerlink" title="Screen 会话"></a>Screen 会话</h2><h3 id="启动-screen-会话"><a href="#启动-screen-会话" class="headerlink" title="启动 screen 会话"></a>启动 screen 会话</h3><ul>
<li>直接执行 screen 命令，将会创建 screen 会话</li>
<li>-U：以 UTF-8 模式运行 screen</li>
<li>使用 <code>-S sessionname</code> 启动选项设置新会话的名称。默认会话名称的格式为 <code>pid.tty.host</code></li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ screen -S <span class="built_in">test</span></span><br><span class="line"></span><br><span class="line">$ screen -ls</span><br><span class="line">There is a screen on:</span><br><span class="line">	25966.test	(Detached)</span><br></pre></td></tr></table></figure>

<h3 id="查看-screen-会话"><a href="#查看-screen-会话" class="headerlink" title="查看 screen 会话"></a>查看 screen 会话</h3><ul>
<li>使用 <code>screen -ls</code> 查看 screen 会话列表</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ screen -ls</span><br><span class="line">There are screens on:</span><br><span class="line">	26540.test2	(Attached)</span><br><span class="line">	25966.test	(Detached)</span><br><span class="line">2 Sockets <span class="keyword">in</span> /var/folders/28/h5qjbptn0fq83y2k55vgvzv8920py5/T/.screen.</span><br></pre></td></tr></table></figure>

<h3 id="detach-会话"><a href="#detach-会话" class="headerlink" title="detach 会话"></a>detach 会话</h3><ul>
<li>在 screen 会话内，可以通过 <code>C-a d</code> 或者 <code>detach</code> 命令（后者需要进入 colon 模式）</li>
<li>在任何地方，都可以通过 <code>screen -d [pid.sessionname]</code> 的方式 detach 指定会话</li>
</ul>
<p>**<code>C-a</code> 代表 <code>Ctrl-a</code>**，它是 Screen 默认的控制键。</p>
<h3 id="attach-会话"><a href="#attach-会话" class="headerlink" title="attach 会话"></a>attach 会话</h3><ul>
<li><code>screen -r [pid.sessionname]</code>：attach 到指定会话</li>
<li><code>screen -d r [pid.sessionname]</code>：attach 到指定会话，必要时先 detach。适用的场景：会话已经被 attach，但是找不到 attach 会话所在的终端，通过该命令可以先 detach，再重新 attach</li>
</ul>
<h2 id="Screen-常用命令"><a href="#Screen-常用命令" class="headerlink" title="Screen 常用命令"></a>Screen 常用命令</h2><table>
<thead>
<tr>
<th>命令</th>
<th>功能</th>
<th>默认快捷键</th>
</tr>
</thead>
<tbody><tr>
<td>colon</td>
<td>进入 colon 模式</td>
<td>C-a :</td>
</tr>
<tr>
<td>help</td>
<td>帮助</td>
<td>C-a ?</td>
</tr>
<tr>
<td>bind [-c class] key [command [args]]</td>
<td>给命令设置快捷键</td>
<td></td>
</tr>
<tr>
<td>bindkey [opts] [string [cmd args]]</td>
<td>绑定按键序列到一个字符串</td>
<td></td>
</tr>
<tr>
<td>sessionname [name]</td>
<td>重新设置当前会话的名称</td>
<td></td>
</tr>
<tr>
<td>detach</td>
<td>detach Screen 会话</td>
<td>C-a d</td>
</tr>
<tr>
<td>quit</td>
<td>关闭所有窗口，退出当前 session</td>
<td></td>
</tr>
<tr>
<td>windowlist -b</td>
<td>列出窗口列表以供选择</td>
<td>C-a “</td>
</tr>
<tr>
<td>select</td>
<td>在窗口间进行选择</td>
<td>C-a’</td>
</tr>
<tr>
<td>select 0. . . select 9, select -</td>
<td>选择窗口 0…9 以及 blank window</td>
<td>C-a 0…9, -</td>
</tr>
<tr>
<td>prev</td>
<td>回到上一个窗口</td>
<td>C-a p</td>
</tr>
<tr>
<td>next</td>
<td>回到到下一个窗口</td>
<td>C-a n</td>
</tr>
<tr>
<td>other</td>
<td>回到之前所在窗口</td>
<td>C-a C-a</td>
</tr>
<tr>
<td>info</td>
<td>显示当前窗口的信息</td>
<td>C-a i</td>
</tr>
<tr>
<td>number</td>
<td>显示当前窗口的 number</td>
<td>C-a N</td>
</tr>
<tr>
<td>windows</td>
<td>显示当前活跃的窗口</td>
<td>C-a w</td>
</tr>
<tr>
<td>screen</td>
<td>创建一个新的窗口并切换到该窗口</td>
<td>C-a c</td>
</tr>
<tr>
<td>kill</td>
<td>关闭当前窗口</td>
<td>C-a k</td>
</tr>
<tr>
<td>collapse</td>
<td>重新排列窗口，消除窗口编号 gap</td>
<td></td>
</tr>
<tr>
<td>shell command</td>
<td>设置创建 shell 的命令</td>
<td></td>
</tr>
<tr>
<td>title</td>
<td>修改当前窗口的 title</td>
<td>C-a A</td>
</tr>
<tr>
<td>chdir [directory]</td>
<td>为之后的窗口设置工作目录</td>
<td></td>
</tr>
<tr>
<td>meta</td>
<td>在窗口中发送 C-a</td>
<td>C-a a</td>
</tr>
<tr>
<td>clear</td>
<td>清屏</td>
<td>C-a C</td>
</tr>
<tr>
<td>log</td>
<td>开启当前窗口的 log，并保存到 screenlog.n</td>
<td>C-a d</td>
</tr>
<tr>
<td>split</td>
<td>将当前 region 水平划分成两个 region</td>
<td>C-a Q</td>
</tr>
<tr>
<td>split -v</td>
<td>将当前 region 垂直划分成两个 region</td>
<td>C-a</td>
</tr>
<tr>
<td>focus [ next</td>
<td>prev</td>
<td>up</td>
</tr>
<tr>
<td>remove</td>
<td>移除当前 region</td>
<td>C-a X</td>
</tr>
<tr>
<td>only</td>
<td>只留下当前 region</td>
<td>C-a Q</td>
</tr>
<tr>
<td>fit</td>
<td>根据当前 region 调整窗口大小</td>
<td>C-a F</td>
</tr>
<tr>
<td>copy</td>
<td>进入 copy/scrollback 模式</td>
<td>C-a [</td>
</tr>
<tr>
<td>paste</td>
<td>进入 paste</td>
<td>C-a ]</td>
</tr>
<tr>
<td>history</td>
<td>copy 之前的命令</td>
<td>C-a {</td>
</tr>
</tbody></table>
<h2 id="Tips"><a href="#Tips" class="headerlink" title="Tips"></a>Tips</h2><ul>
<li><p>在 detach 会话后，会话中的 region 布局并不会自动保存。可以通过创建 <code>layout</code> 来保存布局。但是在我的环境中并没有使用成功？</p>
</li>
<li><p>screen 允许我们直接 copy/paste 窗口中的内容。在进入 copy/scrollback 模式后，可以通过类似于 vim 的命令进行光标移动。复制范围通过两个 mark 进行设置，两个 mark 之间的本文被复制。通过 <code>space</code> 或 <code>enter</code> 设置 mark。之后直接就可以通过粘贴命令粘贴选中文本</p>
</li>
<li><p>在 iterm2 上使用时碰到一个问题，鼠标滚动并没有滚动屏幕，而是滚动了 bash 命令行历史，此时可以尝试将 iterm2 的 <code>Disable save/restore alternate screen</code> 勾选上，问题得到解决</p>
</li>
</ul>
<h2 id="screenrc"><a href="#screenrc" class="headerlink" title=".screenrc"></a>.screenrc</h2><p>在<a href="https://github.com/fuchencong/dotfiles/blob/master/.screenrc">这里</a>访问我的 <code>.screenrc</code>。</p>
<h2 id="Reference"><a href="#Reference" class="headerlink" title="Reference"></a>Reference</h2><ul>
<li><a href="https://www.gnu.org/software/screen/manual/screen.pdf">Screen Manual</a></li>
</ul>
]]></content>
      <categories>
        <category>开发工具</category>
      </categories>
      <tags>
        <tag>Screen</tag>
      </tags>
  </entry>
  <entry>
    <title>看见网络-《Wireshark 网络分析系列》读书笔记</title>
    <url>/fuchencong.github.io/2021/06/19/wireshark-01/</url>
    <content><![CDATA[<p>本篇文章是对林沛满老师的两部书籍《Wireshark 网络分析就这么简单》和《Wireshark 网络分析的艺术》的读书笔记。这两本书详实地记录了作者使用 Wireshark 来分析各类网络疑难杂症的经历。通过这些案例，作者介绍了许多使用 Wireshark 的小技巧，同时也向我们传授了其网络排障的一些宝贵经验。阅读这两本书时，让我回想起当时面试百度时面试官的一个问题：”客户端访问服务器慢，怎么排查？”。如果之前我阅读过这两本书，我想我可以回答地更详尽一些。</p>
<p>Wireshark 是解决网络问题的瑞士军刀，通过 Wireshark，我们可以清楚地看见网络中的每一个数据包（此刻请自行脑补你站在世界地图面前，清楚看见当前 Internet 上每一个数据包的流动）。</p>
<span id="more"></span>

<h2 id="Wireshark-简介"><a href="#Wireshark-简介" class="headerlink" title="Wireshark 简介"></a>Wireshark 简介</h2><p>Wireshark 是目前非常流行的、跨平台的网络嗅探器（sniffer），其提供了非常友好的图形界面。同时如果你是命令行 Geeker，也可以使用它的命令行形式 TShark。Wireshark 有两大作用：</p>
<ul>
<li>协助网络工程师定位网络问题</li>
<li>帮助网络工程师更深刻地理解网络协议</li>
</ul>
<h2 id="Wireshark-使用"><a href="#Wireshark-使用" class="headerlink" title="Wireshark 使用"></a>Wireshark 使用</h2><p>纸上得来终觉浅，觉知此事要躬行。对于工具类软件的学习，最好的办法就是 <code>get hands dirty</code>。以下关于 Wireshark 的使用介绍是基于我 Mac 上的 Wireshark 3.4.3，不同平台、不同版本的 Wireshark 在使用细节上可能略有不同。</p>
<h3 id="抓包"><a href="#抓包" class="headerlink" title="抓包"></a>抓包</h3><p>在抓取网络包时，我们应该尽量只抓取必要的部分。有很多方法可以实现这一点：</p>
<ul>
<li><p>只抓包头：一般一个帧的最大长度为 1514 字节，在启用了 jumbo Frame 之后，帧的大小可达 9000 字节以上。而大多数时候只需要抓取 IP 头或 TCP 头即可。可以在 <code>Capture-&gt;Options-&gt;Input-&gt;Snaplen(B)</code> 中进行设置，例如设置成 80 字节，这样一般就可以包含数据链路层、网络层、传输层的信息了。如果还需要包含应用层，那就需要设置的更大一点。使用 <code>tcpdump</code> 时，其 <code>-s</code> 选项也可以设置抓包大小</p>
</li>
<li><p>只抓必要的包：Wireshark 的 Capture Filter 可以在抓包时过滤掉不需要的包，可以在 <code>Capture-&gt;Options-&gt;Input-&gt;Capture filter</code> 中设置。</p>
</li>
</ul>
<p>例如在我的无线网卡中设置只抓取 53 端口（DNS）的报文，然后执行 <code>nslookup www.baidu.com</code> 就可以看到抓取的 DNS query 和 DNS response 报文：</p>
<img src="/fuchencong.github.io/2021/06/19/wireshark-01/capture_53.png" class="">
<img src="/fuchencong.github.io/2021/06/19/wireshark-01/53_packets.png" class="">

<ul>
<li>抓包时每一步打上标记，这样抓包结果一目了然。例如一个操作涉及三个步骤，可以在每一步之前通过 <code>ping &lt;IP&gt; -n 1 -l $size</code> 的方式发送标记报文，其中 size 就是当前步骤编号。这样我们就可以在一系列报文中区分出每一步所涉及的报文了。</li>
</ul>
<p>另外，如果你在抓包时提示你没有抓包权限，只需要按照 Wireshark 的提示安装 <code>ChmodBPF</code> 软件包即可。</p>
<img src="/fuchencong.github.io/2021/06/19/wireshark-01/no_permission.png" class="">

<h3 id="个性化设置"><a href="#个性化设置" class="headerlink" title="个性化设置"></a>个性化设置</h3><ul>
<li>不同类型的网络包可以自定义颜色，这样就可以一目了然的得到报文分类。通过 <code>View-&gt;Coloring Rules</code> 修改颜色规则</li>
<li>协议的细节设置可以在 <code>Preferences-&gt;Protocols</code> 中进行设置，例如对 tcp 协议的设置项如下：</li>
</ul>
<img src="/fuchencong.github.io/2021/06/19/wireshark-01/tcp_protocol_set.png" class="">

<h3 id="过滤"><a href="#过滤" class="headerlink" title="过滤"></a>过滤</h3><p>除了在抓包的时候可以进行过滤，报文在完成抓取后还可以进一步过滤。很多时候，解决问题的关键就是找到关键报文：</p>
<ul>
<li>如果是某个协议发生问题，可以直接用协议名称过滤</li>
</ul>
<img src="/fuchencong.github.io/2021/06/19/wireshark-01/filter_by_protocol.png" class="">

<ul>
<li><p>IP地址加端口：例如 <code>ip.src_host==127.0.0.1 and tcp.srcport==8000</code></p>
</li>
<li><p>Wireshark 提供了更快捷的方式：在感兴趣的包上单击右键，选择 <code>Follow-&gt;TCP Stream</code> 就可以自动选择该 TCP 流的所有数据包，并且在新窗口中显示该 stream 内容，如下所示</p>
</li>
</ul>
<img src="/fuchencong.github.io/2021/06/19/wireshark-01/follow_tcp_stream.png" class="">

<ul>
<li>Wireshark 根据五元组过滤出 TCP/UDP Stream。通过 <code>Wireshark 的 Statistics-&gt;Conversations</code>，选择 TCP/UDP 标签，就可以看到对应的 TCP/UDP stream 统计信息</li>
</ul>
<img src="/fuchencong.github.io/2021/06/19/wireshark-01/stream_statistics.png" class="">

<ul>
<li><p>当鼠标选中某个报文的某个字段后，通过 <code>Prepare a Filter-&gt;Selected/And Selected/...</code> 等选项就可以将在 Filter 框中自动生成对应的过滤表达式。如果选择的是 <code>Prepare a Filter-&gt;Selected/And Selected/...</code>，则直接应用该过滤条件</p>
</li>
<li><p>通过 <code>File-&gt;Export Specified Packets</code> 按要求保存数据包</p>
</li>
<li><p>Wireshark 也支持按关键字搜索报文，通过 <code>Ctrl+F</code> 进行搜索，搜索内容为 <code>Packet bytes</code>，搜索方式为 <code>String</code>，例如：</p>
</li>
</ul>
<img src="/fuchencong.github.io/2021/06/19/wireshark-01/search_packets.png" class="">

<h3 id="让-Wireshark-自动分析"><a href="#让-Wireshark-自动分析" class="headerlink" title="让 Wireshark 自动分析"></a>让 Wireshark 自动分析</h3><p>有时候我们不需要研究包的细节，可以让 Wireshark 帮我们自动分析：</p>
<ul>
<li>通过 <code>Analyse-&gt;Expert Info</code> 可以看到不同级别的提示信息，例如连接建立、连接终止等信息。在分析网络性能和连接问题时，经常需要借助这个功能</li>
<li>通过 <code>Service-&gt;HTTP/DNS/...</code> 可以查看具体协议的统计信息，例如查看 HTTP 的统计信息</li>
</ul>
<img src="/fuchencong.github.io/2021/06/19/wireshark-01/http_statistics.png" class="">

<ul>
<li>通过 <code>Statistics-&gt;TCP Stream Graph</code> 可以查看 TCP 的统计图</li>
<li>通过 <code>Statistics-&gt;Capture File Properties</code> 可以获取总体的统计信息</li>
</ul>
<h2 id="使用-Wireshark-分析-TCP"><a href="#使用-Wireshark-分析-TCP" class="headerlink" title="使用 Wireshark 分析 TCP"></a>使用 Wireshark 分析 TCP</h2><p>TCP 一直是 TCP/IP 协议栈中非常复杂的一部分，接下来将使用 Wireshark 分析一个典型的 TCP 连接建立/断开过程。这里我使用 <code>curl www.baidu.com</code> 访问百度首页，使用 Wireshark 抓到的报文如下：</p>
<ul>
<li>首先是 TCP 建立所涉及的三个报文，也被称为 TCP 三次握手。可以看到，在前两个报文中，双方都把自己的 MSS（Maximum Segment Size）告诉对方，MSS 为 <code>本端 MTU - IP 头部长度（通常为 20 字节）- TCP 头部长度（通常为 20 字节）</code>。在建立连接时主动通告自己的 MSS，也体现了 TCP 会主动为 IP 层着想，避免报文传递给 IP 层后被 IP 层分片。<strong>但是这种方式并不能完全避免报文分片，因为如果网络路径上存在 MTU 小于通信双方所通告的 MTU 的话，这个包还是会被分片</strong>。</li>
</ul>
<img src="/fuchencong.github.io/2021/06/19/wireshark-01/tcp01.png" class="">

<ul>
<li>接来的报文则是应用层通信过程，即一个典型的 HTTP 交互过程，客户端发送 HTTP GET 请求（4 号包），服务器返回 HTTP 响应（6，7，8）。另外，5 号包是服务器端 TCP 层对所接收到的 4 号包的 ACK 报文。服务器的 HTTP 响应一共通过 3 个报文发送，所有报文的 TCP 数据部分长度都不超过 1370（即通信双方的 MSS 中的较小值，本例中服务端的 MSS 为 1370，客户端通告的 MSS 为 1460，所以通信双方 TCP 数据部分长度都不会超过 1370）。9、10 号包是客户端 TCP 层对所接收到的 HTTP 响应数据的 ACK 报文，其中 9 号包是对 6、7 号包的确认，10 号包是对 8 号包的确认。11 号包是客户端的 TCP 窗口更新通告，这侧面反映此时客户端程序从 TCP socket buffer 里将 HTTP 响应数据读走了，所以接收端窗口恢复成初始值 262144</li>
</ul>
<img src="/fuchencong.github.io/2021/06/19/wireshark-01/tcp02.png" class="">

<ul>
<li>最后 4 个报文则是典型的 TCP 连接断开过程，也被称为 <code>TCP 四次挥手过程</code>。</li>
</ul>
<img src="/fuchencong.github.io/2021/06/19/wireshark-01/tcp03.png" class="">

<p>从整个 TCP 报文交互过程我们也可以看到，SYN包、FIN 包都需要占据一个发送序号，而单纯的 ACK 包是不占发送发送序号的。另外，这里看到的报文序号是从 0 开始，这是因为 Wireshark 启用了 <code>Relative Sequence Number</code> 功能，真实的序号并不一定是从 0 开始。</p>
<h3 id="Wireshark-定位-TCP-三次握手-四次挥手相关问题"><a href="#Wireshark-定位-TCP-三次握手-四次挥手相关问题" class="headerlink" title="Wireshark 定位 TCP 三次握手/四次挥手相关问题"></a>Wireshark 定位 TCP 三次握手/四次挥手相关问题</h3><p>握手失败一般分为两种类型：要么被拒绝，要么是丢包了。使用如下两个过滤表达式可以定位大多数失败的握手</p>
<ul>
<li><code>(tcp.flags.reset == 1) &amp;&amp; (tcp.seq == 1)</code>：过滤出所有含有 Reset 标志的相对序号为 1 的报文</li>
<li>(tcp.flags.syn == 1) &amp;&amp; (tcp.analysis.retransmission)：过滤出重传的握手请求。重传有可能是 SYN 报文丢弃，也有可能是对端恢复的 ack 报文丢弃。</li>
</ul>
<p>通过 Wireshark 的 <code>Analyse-&gt;Expert Info-&gt;Chats</code> 可以迅速发现是否遭遇了 SYN 泛洪攻击。</p>
<p>TCP 的延迟确机制，可能导致 TCP 的挥手过程只需要 3 个报文即可完成，此时第二个报文为 FIN/ACK 包。</p>
<h3 id="TCP-窗口"><a href="#TCP-窗口" class="headerlink" title="TCP 窗口"></a>TCP 窗口</h3><p>TCP 为应用层提供可靠的传输机制，但是如果发送端每发送一个包就停下来确认，那么一个往返时间里只能传一个包，这样传输效率太低了。最快的方式是一口气把所有包都发出去，然后一起确认。但是现实中也存在一些其他限制，例如接收方的缓存（接收窗口）无法一下子接收太多数据；或者网络的带宽也是有限的，发送端一口气将报文全部发出来可能会导致丢包。所以发送方需要知道接收窗口和网络两个限制因素哪一个更严格，然后在其限制范围内尽可能多发包。一口气能够发送的数据量就是传说中的 TCP 发送窗口。</p>
<ul>
<li>每个 TCP 报文的头部都包含 <code>Window Size</code> 字段，这是在向对端通告自己的接收窗口大小（<strong>并没有办法从 TCP 报文中得知发送端的发送窗口大小</strong>）。对端收到该通告后，就会把自己的发送窗口限制在该范围内，这就是 TCP 滑动窗口机制的一部分。</li>
<li>发送窗口决定了一口气能够发送多少字节，MSS 决定了这些字节要分多少个包发完。</li>
<li>TCP 头部中 <code>Window Size</code> 只占 16 位，所以最大值只能为 65535。而现在随着网络的进步，该窗口大小已经远远无法满足应用程序对性能的要求。RFC1323 提出了一个解决方案：在 TCP 三次握手过程中，将 <code>Window Scale</code> 放在 TCP 头部选项中，<code>Window Scale</code> 用于向对方声明一个偏移量，以该偏移量进行 2 的指数运算，即得到 Scale 值。将 <code>Window Size</code> 乘以该 Scale 值即为最终的 <code>Window Size</code>。如下展示了上个例子中服务器 SYC-ACK 报文中所通告的 <code>Window Scale</code> 值：</li>
</ul>
<img src="/fuchencong.github.io/2021/06/19/wireshark-01/window_scale.png" class="">

<ul>
<li><code>Window Scale</code> 只会在建立连接时进行通告，之后报文交互过程中，都需要将报文中所通告的 <code>Window Size</code> 乘以之前对端所通告的 <code>Window Scale</code>，才是对端当前真实的接收窗口大小。Wireshark 在显示 <code>Win</code> 时也是根据这一方法计算的，所以如果抓包时没有抓到三次握手，Wireshark 就不知道该如何计算，这时可能就会看到一些极小的接收窗口值</li>
</ul>
<p>决定发送窗口的因素有两个：</p>
<ul>
<li>网络上的拥塞窗口（Congestion Window，缩写为 cwnd）</li>
<li>接收端的通告的接收窗口</li>
</ul>
<p>发送端的输出不能超过 cwnd 和接收方通告窗口的大小。拥塞避免是发送方使用的流量控制，而通告窗口则是接收方进行的流量控制。前者是发送方感受到的网络拥塞的估计，而后者则与接收方在该连接上的可用缓存大小有关。</p>
<p>cwnd 的增长方式是先 <code>慢启动</code>，然后再是 <code>拥塞避免</code>。前者起点低但能快速增长，后者起点高，但是每个 RTT 只能增加一个 MSS。在 Wireshark 中，选中一个发送窗口中最后的一个包，看到它的 <code>Bytes in flight</code>，有时它就可以代表 cwnd 的大小（当发送数据量没有达到接收窗口大小时，但却没有继续发送，说明此次发送端受 cwnd 的影响）。</p>
<p>理论上只要窗口足够大，TCP 也可以不受往返时间的约束而源源不断地传送数据。当然 TCP 确实也有因为往返时间而降低效率的时候，例如传输小块数据，本来一个 RTT 时间就可以完成的事情，却需要额外的 3 次握手和 4 次挥手。Google 的 QUIC（Quick UDP Internet Connection）协议就是为了消除 TCP 的延迟而设计的替代品。</p>
<h3 id="TCP-重传"><a href="#TCP-重传" class="headerlink" title="TCP 重传"></a>TCP 重传</h3><p>网络因素之所以会影响发送窗口，是因为如果网络一下子收到太多数据时，就会发生拥塞，拥塞的结果就是丢包，那么这个时候接收端可能就无法对报文进行确认，发出的报文一直缓存在发送窗口中，等待重传。这个时候应用程序的可用发送窗口也就受限。能够导致网络拥塞的数据量称为拥塞点，发送方应该将发送窗口控制在拥塞点以下，这样就能避免拥塞了。但是拥塞点并不容易计算，因为传输路径中可能存在多个网络设备，其中任意一个设备都可能是瓶颈。而且拥塞点是一个随时会改变的动态值，这就更加不好估算了。</p>
<p>为了解决该问题，就有了 TCP 的拥塞避免机制。它的策略就是在发送方维护一个虚拟的拥塞窗口，并利用各种算法使他尽可能接近真实的拥塞点。网络对发送窗口的限制，就是通过拥塞窗口实现的：</p>
<ul>
<li>连接刚刚建立的时候，发送方对网络状况一无所知，如果一口气发送太多就可能遭遇拥塞，所以发送方把拥塞窗口的初始值定的很小，RFC 的建议是 2 个、3 个或者 4 个 MSS，具体根据 MSS 大小而定</li>
<li>如果发出去的包都得到确认，表明还没有到达拥塞点，可以增大拥塞窗口。由于该阶段发生拥塞的概率很低，所以增速应该快一些。RFC 建议的算法是每收到 n 个确认，就可以把拥塞窗口增加 n 个 MSS。这个过程增速很快，但是由于基数较低，传输速度还是比较慢的，所以称为慢启动过程</li>
<li>慢启动过程持续一段时间后，拥塞窗口达到一个较大值，这时候传输速度较快，触碰到拥塞点的概率比较大，所以不能再继续采用翻倍的慢启动算法了，而是要缓慢一点，，RFC 建议的算法是在每个往返时间增加 1 个 MSS，这个过程称为拥塞避免。从慢启动过渡到拥塞避免的临界窗口值很有讲究：如果之前发送过拥塞，就将该拥塞点作为参考依据，如果从来没有拥塞过就可以取相对较大的值，比如和最大接收窗口相等</li>
</ul>
<p>如果发生拥塞，对发送方来说，就是发出去的包不像往常一样得到确认了。但是收不到确认也可能是网络延迟所导致的。所以发送方会等待一段时间再判断，如果一段时间后仍然没有收到，就认为该包丢失了，只能重传了。这个过程称为超时重传。从发出原始包到重传该包的时间称为 RTO。重传之后需要重新调整拥塞窗口，为了不给刚发生拥塞的网络雪上加霜，RFC 建议把拥塞窗口降到一个 1 个 MSS，然后再次启动慢启动过程，此时从慢启动过渡到拥塞避免的临界窗口值就有参考依据了。 RFC 5681 认为应该是发生拥塞时没有被确认数量的 1/2，但不能小于 2 个 MSS。</p>
<p>超时重传对性能有严重影响：</p>
<ul>
<li>RTO 阶段不能传数据，相当于浪费了一段时间</li>
<li>拥塞窗口急剧减小，相当于接下来传得慢的多了</li>
</ul>
<p>通过 Wireshark 的 <code>Analyse-&gt;Expert Info</code> 可以看到重传情况。</p>
<p>当拥塞较小导致少量包丢失时，或者偶然因素导致校验码不对时，会导致单个丢包。这两种丢包和严重拥塞时不一样，因为后续有包能正常到达，当后续的包到达时，接收方会发现其 Seq 比期望的大，所以每收到一个包就 Ack 一次期望的 Seq 序号，以提醒发送方重传。当发送方收到 3 个或以上重复确认时，就意识到丢包了，从而立即重传它，这个过程称为快速重传，它不需要像超时重传一样需要等待一段时间。</p>
<p>之所以需要凑满 3 个，是因为网络包有时会乱序，乱序的包一样会触发重复的 Ack，但是为了乱序而重传没有必要。一般乱序的距离不会相差太大，所以限定 3 个或以上可以很大程度上避免因为乱序而触发快速重传。</p>
<p>如果在拥塞避免阶段发生了快速重传，不会像发生超时重传一样处理拥塞窗口。因为完全没有必要。既然后续的包都到达了，说明网络并没有严重拥塞，接下来传慢点就可以了。RFC5681 建议临界窗口值应该设置为发生拥塞时还没有被确认数据量的 1/2（但不能小于 2 个 MSS），然后将拥塞窗口设置为临界窗口值加 3 个 MSS，继续保留在拥塞避免阶段，这个过程称为快速恢复。</p>
<p>很多时候丢的包不止一个，而通过接收方的 ACK 序号只能知道该序号的包肯定需要重传，但并不知道还有哪些包也丢失了，此时有几种方案：</p>
<ul>
<li>将发送窗口中未确认的包都重传一遍，效率较低（丢一个包导致多个包被重传），早期的 TCP 协议就是这样处理的</li>
<li>只重传相应序号的包，当丢包量较大时，需要花费很多个 RTT 时间来重传所有丢失的包</li>
<li>接收方在发送 Ack 包时，顺便把收到的包号告诉发送方，这样发送方对丢包细节了如指掌。这个方案称为 SACK，由 RFC2018 定义。</li>
</ul>
<p>最后介绍了一个经验，丢包对极小文件的影响比大文件严重。因为读写一个小文件需要的包数据很少，所以丢包时往往凑不满 3 个 Dup ACK，只能等待超时重传了，而大文件有较大可能触发快速重传。</p>
<p>网络承载量可以用已经发送出去，但是尚未确认的字节数来表示（bytes in flight，在途字节数）。在途字节数如果超过了网络的承载能力，也会丢包重传。在 Wireshark 中通过某一个时间点的发包序号、已被确认序号计算得知：<code>在途字节数 = Seq + Len - Ack</code>。当发送方一口气向网络中注入大量数据时，就可能超过该网络的承受能力而导致拥塞，这个足以触发拥塞的数据量就称为拥塞点。大致可以认为：发生拥塞时的在途字节数即是该时刻的网络拥塞点。</p>
<p>拥塞的特征就是连续丢包，丢包就会重传，而 Wireshark 是可以标识出重传报文的。因此：先从 Wireshark 中找到一连串重传包中的第一个，在根据该重传包的 seq 值找到其原始包，最后计算该原始包发送时刻的在途字节数。由于网络拥塞就是在该原始包发出去的时刻发生的，所以在途字节数就大致代表了拥塞点的大小。该方法不一定准确，但是有参考意义，最好多次采样，然后选定一个合适的值作为拥塞点。</p>
<p>在局域网中，当重传率超过 0.1% 就值得采取措施了。</p>
<h3 id="延迟确认"><a href="#延迟确认" class="headerlink" title="延迟确认"></a>延迟确认</h3><p>TCP 的延迟确认原理是：如果收到一个包之后暂时没有什么数据要发送给对方，那么就延迟一段时间再确认。如果在这段时间里恰好有数据要发送，那确认信息和数据就可以在一个包里发出去。延迟确认并没有直接提高性能，它只是减少了部分确认包，减轻了网络负担。而且有时候延迟确认反而会影响性能。</p>
<p>例如在某些古老的 TCP 协议栈在处理网络拥塞时，可能会出现如下情况：</p>
<ul>
<li>假设客户端在同一发送窗口发送了 9 个 TCP 包，但是 3、4、5 号因为拥塞丢失了</li>
<li>到达服务器的 6、7、8、9 触发了 4 个 <code>Ack 3</code>，于是客户端迅速重传报文 3</li>
<li>服务器收到重传的 3 号报文后，由于启用了延迟确认，所以等待 200ms 才恢复 Ack 4</li>
<li>客户端重传 4 号包，服务器又等待 200ms 后才回复 Ack 5</li>
<li>客户端重传 5 号包，服务器又等待 200ms 后才回复 Ack 10</li>
</ul>
<p>所以此时：在连续丢掉多个包后，由于延迟确认，每个重传都需要等待 200ms。所以延迟确认和重传放在一起，可能会影响性能。</p>
<p>对于上述问题，启用 TCP SACK（Selective Acknowledgement）可以解决该问题。通过服务器的 SACK，客户端可以一次性重传多个丢包，而不用每重传一个就等待一次 ACK，白白浪费多个往返时间。</p>
<p>延迟确认导致性能下降的另外一种场景是：TCP 窗口极小的情况下，发送端每次发送若干个 MSS 后就会耗光窗口，若此时再开启延迟确认，那么每次发送端都需要等待 200ms 后（收到对端的 Ack）才可以继续发送。</p>
<p>在 Wireshark 中 使用 <code>tcp.analysis.ack_rtt &gt; 0.2 and tcp.len==0</code> 可以将超过 200毫秒的确认都筛选出来。</p>
<h3 id="Nagle-算法"><a href="#Nagle-算法" class="headerlink" title="Nagle 算法"></a>Nagle 算法</h3><p>Nagle 算法的原理是：在发出去的数据还没有确认前，假如又有小数据生成，那么把小数据收集起来，凑满一个 MSS 或者等收到确认后再发送。和延迟确认一样，Nagle 也没有直接提高性能，启用它的作用是提高传输效率，减轻网络负担。在某些场合下，和延迟确认一起使用时甚至会降低性能（发送端由于需要凑慢一个 MSS 而没有立即发送数据，而对端又开启了延迟确认，导致 Ack 迟迟不来，导致应用层数据一直没有得到发送，从而导致性能降低）。</p>
<h3 id="LSO-与-LRO"><a href="#LSO-与-LRO" class="headerlink" title="LSO 与 LRO"></a>LSO 与 LRO</h3><p>LSO（Large Segment Offload）是为了拯救 CPU 而出现的一个创意，传统网络的工作方式是：应用层把产生的数据交给 TCP 层，TCP 层再根据 MSS 的大小进行分段（由 CPU 负责），然后再交给网卡，而启用 LSO 之后，TCP 层就可以把大于 MSS 的数据块直接传给网卡，让网卡来负责分段工作。而发送方 Wireshark 抓包时是站在 CPU 的视角，所以可能看到是一个分段前的大包。</p>
<p>LRO（Large Receive Offload），会积累多个 TCP 包再集中处理，此时可能造成 Ack 数目小于其他机器（在没有开启延迟确认的情况下，并不是所有的 TCP/IP 实现都是收到 n 个数据包就回复 n 个 Ack 的，例如很多 Linux 服务器就是累计收到两个数据包才 Ack 一次）。</p>
<h3 id="多-TCP-连接问题"><a href="#多-TCP-连接问题" class="headerlink" title="多 TCP 连接问题"></a>多 TCP 连接问题</h3><p>因为网络延迟、拥塞和应用层设计问题，单个连接可能无法占满整个物理链路，此时可以通过增加一些连接数来占满链路。但是当连接数多到足以占满整个链路时，再增加连接就没有意义了，甚至可能会带来负面效果（可能会增加丢包率）。当 Wireshark 提示 [TCP window Full]，此时表明发送方发送的数据量已经达到了对方所声明的接收窗口大小，不能再发了，此时只好停下来等待。这说明带宽没有完全利用，应该继续增加发送窗口或者连接数来补充。</p>
<h2 id="Wireshark-的其他事项"><a href="#Wireshark-的其他事项" class="headerlink" title="Wireshark 的其他事项"></a>Wireshark 的其他事项</h2><ul>
<li>受损的帧不会在 Wireshark 中显示。数据链路层的 FCS（Frame Check Sequence）机制就会直接丢弃受损的帧。通过 <code>netstat -i | column -t</code> 可以直接查看 Linux 上 FCS 错误统计</li>
<li><code>Packet size limited during capture</code>：表示被标记的包没有抓全，一般是由抓包方式引起的，tcpdump 默认只抓每个帧的前 96 个字节，可以使用 <code>-s</code> 来指定抓包字节数</li>
<li><code>TCP Previous segment not captured</code>：缺失的数据包在整个网络包（排除乱序后）都找不到，就会提示该信息</li>
<li><code>TCP ACKed unseen segment</code>：当 Wireshark 发现被 Ack 的包没有被抓到，就会提示该信息。一般是由于抓包开始时错误过了前面的数据包</li>
<li><code>TCP Out-of-Order</code>：乱序数据包，小跨度的乱序影响不大，但是大跨度的乱序却可能重发快速重传</li>
<li><code>TCP Dup ACK</code>：当乱序或丢包时，接收方会收到一些 Seq 号比期望值大的包。它每收到一个这种包时，就会 Ack 一次期望的 Seq 值，以此来提醒发送方，于是就产生了重复的 Ack。</li>
<li><code>TCP Fast Retransmission</code>：当发送方收到 3 个以上 <code>TCP Dump Ack</code> 就意识到之前发的包可能丢了，于是快速重传它</li>
<li><code>TCP Retransmission</code>：如果一个包真的丢了，但是又没有后续包可以在接收方触发 <code>Dup Ack</code>，就不会触发快速重传。此时只能等到发送方的超时重传了，该类包被 Wireshark 标记为 <code>TCP Retransmission</code></li>
<li><code>TCP zerowindow</code>：TCP 包中的 <code>win=</code> 表示接受窗口的大小，即表示这个包的发送者当前还有多少缓存区可以接收数据。当 <code>win=0</code> 时，Wireshark 就会给该报文打上<code>TCP zerowindow</code> 标志，表示缓冲区已满，不能再接收数据</li>
<li><code>TCP window Full</code>：表示这个包的发送者已经把对方声明的接收窗口耗尽了，即 <code>在途字节数</code> 已经达到最大值了</li>
<li><code>TCP segment of a reassembled PDU</code>：这说明开启了 <code>Allow sub dissector to reassemble TCP stream</code>，它表示 Wireshark 可以把属于同一个应用程序 PDU 的 TCP 包虚拟地集中起来。<strong>Wireshark 在最后一个包中虚机地把所有包集中起来</strong></li>
<li><code>Continuation to #</code>：这说明没有开启 <code>Allow sub dissector to reassemble TCP stream</code>，这时同一个应用程序 PDU 的包没有被 Wireshark 集中起来</li>
<li><code>Time-to-live exceeded (Fragment reassembly time exceeded)</code>：ICMP 错误之一，表示这个包的发送方之前收到了一些分片，但是由于某些原因迟迟无法组装起来</li>
<li>如果服务器上的多个网卡被绑定为一个 NIC Teaming，需要注意它的类型。如果为 TLB（Transmit Load Balancing）则收包由一个网卡负责，发包则分摊给所有网卡。</li>
<li>每经过一次路由，网络包的 TTL 值就减去 1，有时网络包的 TTL 值也有助于定位网络问题，TTL 的初始值值一般为 64。</li>
</ul>
<h2 id="tshark"><a href="#tshark" class="headerlink" title="tshark"></a>tshark</h2><p>tshark 是 wireshark 的命令行版本，和图形界面相比，命令行有一些先天的优势：</p>
<ul>
<li>命令行的输出可以直接通过 awk 之类的方式处理处理，这是图形界面难以实现的</li>
<li>编辑命令虽然费时，但是编辑好后可以反复使用</li>
<li>tshark 输出的分析文本大多数可以直接写入分析报告中，而 Wireshark 生成不了这样的报告</li>
<li>命令行往往比图形界面快的多</li>
</ul>
]]></content>
      <categories>
        <category>Network</category>
      </categories>
      <tags>
        <tag>Wireshark</tag>
      </tags>
  </entry>
</search>
